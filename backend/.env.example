# Variables de entorno para la aplicación

# Puerto donde se ejecutará el servidor
# En producción, Render establecerá automáticamente la variable PORT
PORT=5000

# Configuración de la base de datos
# (En este caso no es necesario porque usamos SQLite)

# Configuración de correo (para el formulario de contacto)
# Necesitarás una cuenta de Gmail y generar una contraseña de aplicación
EMAIL_USER=tu_correo@gmail.com
EMAIL_PASS=tu_contraseña_de_aplicacion
EMAIL_FROM=tu_correo@gmail.com

# Clave secreta para JWT (si se implementa autenticación con tokens)
JWT_SECRET=clave_secreta_para_tokens

# Límites de tasa (rate limiting)
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX=100

# Nota: En Render, estas variables deben configurarse en la sección "Environment" 
# al crear el Web Service, no necesitas crear un archivo .env real

# Configuración de base de datos (no se usa actualmente)
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=floreria
DB_USERNAME=root
DB_PASSWORD=

# Configuración de correo
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=arreglosvictoriafloreria@gmail.com
SMTP_PASSWORD=
SMTP_ENCRYPTION=tls

MAIL_FROM_ADDRESS=arreglosvictoriafloreria@gmail.com
MAIL_FROM_NAME="Arreglos Victoria Florería"
MAIL_TO_ADDRESS=arreglosvictoriafloreria@gmail.com
MAIL_TO_NAME="Arreglos Victoria Florería"

# Configuración de la aplicación
APP_NAME="Arreglos Victoria Florería"
APP_ENV=local
APP_DEBUG=true